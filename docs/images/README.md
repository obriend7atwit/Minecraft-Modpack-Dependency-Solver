# Project Images

`issue-success-heatmap.png` and `runtime-complexity.png` are copied from the archived final evaluation in `results/final/paper/`.

GUI screenshots were not available in this archive. After launching the revised application on a desktop, capture these real screenshots without editing the interface content:

1. `gui-simple.png`: the default window with `missing_required_dependency.json` analyzed and its repair plan visible.
2. `gui-advanced.png`: the same analysis with **Show advanced details** enabled and the Full Explanation tab visible.

The root README intentionally does not link these files until genuine captures exist.
