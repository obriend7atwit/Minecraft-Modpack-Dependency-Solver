# Paper Handoff

The completed paper PDF, complete LaTeX source, and bibliography were not present in the supplied repository archive, so no paper files were invented during cleanup.

Add the final materials as:

- `paper/final_paper.pdf`
- `paper/source/` for the complete compilable source and `.bib` files
- `paper/figures/` for paper-specific source figures when they are not generated directly

Paper-ready generated tables, fragments, and figures currently remain in `../results/final/paper/`. After adding the source, document its exact build command here and verify all relative figure paths.
