# Modified Real Cases

This directory is reserved for future generated fixtures derived from reviewed original-real controls. Current reduced modified-real fixtures remain in `data/evaluation/cached_real/` and have corresponding logs in `../injection_logs/`.
