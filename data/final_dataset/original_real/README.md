# Original Real Cases

This directory is reserved for normalized future full-pack cases. The current manifest references reduced cached examples in `data/evaluation/cached_real/` so the original Week 8/9 fixtures remain unchanged.
