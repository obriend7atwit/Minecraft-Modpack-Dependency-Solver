# Synthetic Fixtures

This directory contains small deterministic JSON scenarios used to validate the project’s internal data models and metadata parsing layer.

These fixtures are intentionally solver-agnostic for now. They are designed so later compatibility-checker and repair tests can target clearly named scenarios such as missing dependencies, loader mismatches, conflicts, and embedded dependencies.
